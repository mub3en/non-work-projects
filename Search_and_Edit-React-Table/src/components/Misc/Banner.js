const Banner = ({ userName }) => (
    <div  style={{'backgroundColor': 'blue', color: 'white' }}>
        <h4 >Writing Emphasis</h4>
        <h4 >for {userName}</h4>
    </div>
);

export default Banner;

